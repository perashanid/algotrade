# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/trading_platform
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trading_platform
DB_USER=username
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# API Configuration
PORT=3001
NODE_ENV=development

# External APIs
FINNHUB_API_KEY=your-finnhub-api-key-here
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-api-key-here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Vercel-specific (set these in Vercel dashboard)
# VERCEL_URL will be automatically set by Vercel
# DATABASE_URL should point to your production database (e.g., Supabase, PlanetScale, etc.)